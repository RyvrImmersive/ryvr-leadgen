{
  "name": "ryvr-leadgen",
  "version": "1.0.0",
  "description": "RYVR RevOps lead gen landing page",
  "scripts": {
    "build": "echo 'Static site — no build step required'",
    "start": "npx serve -s . -l ${PORT:-3000}"
  },
  "dependencies": {
    "serve": "^14.2.3"
  }
}
